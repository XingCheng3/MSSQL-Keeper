<Window x:Class="DBKeeper.App.Dialogs.TaskExecutionHistoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="任务执行历史" Height="560" Width="980"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock x:Name="txtTitle" FontSize="20" FontWeight="SemiBold" />
            <TextBlock x:Name="txtSubTitle" Margin="0,6,0,0"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <DataGrid x:Name="historyGrid" Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="开始时间" Binding="{Binding StartedAtDisplay}" Width="150" />
                <DataGridTextColumn Header="触发" Binding="{Binding TriggerDisplay}" Width="80" />
                <DataGridTextColumn Header="状态" Binding="{Binding StatusDisplay}" Width="80" />
                <DataGridTextColumn Header="耗时" Binding="{Binding DurationDisplay}" Width="90" />
                <DataGridTextColumn Header="摘要" Binding="{Binding SummaryDisplay}" Width="2*" />
                <DataGridTextColumn Header="文件状态" Binding="{Binding FileStatusDisplay}" Width="180" />
                <DataGridTextColumn Header="错误" Binding="{Binding ErrorDisplay}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="2" Margin="0,16,0,0">
            <TextBlock x:Name="txtEmpty" VerticalAlignment="Center"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <ui:Button Content="关闭" Width="96" HorizontalAlignment="Right"
                       DockPanel.Dock="Right" Click="Close_Click" />
        </DockPanel>
    </Grid>
</Window>
