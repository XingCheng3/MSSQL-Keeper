<ui:FluentWindow x:Class="DBKeeper.App.Dialogs.TaskExecutionHistoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="任务执行历史" Height="620" Width="1080"
        MinHeight="520" MinWidth="860"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        ExtendsContentIntoTitleBar="False">

    <Grid TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
          Background="{DynamicResource ApplicationBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="20,12,20,8" MouseLeftButtonDown="DragBar_MouseLeftButtonDown">
            <DockPanel>
                <TextBlock x:Name="txtTitle" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
                <ui:Button DockPanel.Dock="Right" ToolTip="刷新" Margin="8,0,0,0"
                           Icon="{ui:SymbolIcon ArrowClockwise24}"
                           Click="Refresh_Click" />
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Margin="20,8,20,12" Style="{StaticResource DbkInlineDetailsBorderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="txtSubTitle" FontSize="12"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="按任务追溯最近执行情况；备份任务会显示对应文件当前是否还在磁盘上。"
                               Margin="0,4,0,0" FontSize="11" TextWrapping="Wrap"
                               Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                </StackPanel>
                <TextBlock x:Name="txtCount" Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center"
                           FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="20,0,20,0">
            <DataGrid x:Name="historyGrid"
                      Style="{StaticResource DbkDataGridStyle}"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      RowDetailsVisibilityMode="VisibleWhenSelected">
                <DataGrid.RowDetailsTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource DbkInlineDetailsBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{Binding SummaryDisplay}" TextWrapping="Wrap"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                <TextBlock Text="{Binding ErrorDisplay}" Margin="0,6,0,0" TextWrapping="Wrap"
                                           Foreground="{DynamicResource ErrorBrush}"
                                           Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </DataGrid.RowDetailsTemplate>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="开始时间" Binding="{Binding StartedAtDisplay}" Width="155" />
                    <DataGridTextColumn Header="完成时间" Binding="{Binding FinishedAtDisplay}" Width="155" />
                    <DataGridTextColumn Header="触发" Binding="{Binding TriggerDisplay}" Width="70" />
                    <DataGridTextColumn Header="状态" Binding="{Binding StatusDisplay}" Width="75" />
                    <DataGridTextColumn Header="耗时" Binding="{Binding DurationDisplay}" Width="85" />
                    <DataGridTextColumn Header="摘要" Binding="{Binding SummaryDisplay}" Width="2*" />
                    <DataGridTextColumn Header="文件状态" Binding="{Binding FileStatusDisplay}" Width="210" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel x:Name="emptyState" VerticalAlignment="Center" HorizontalAlignment="Center"
                        Visibility="Collapsed">
                <ui:SymbolIcon Symbol="ClockDismiss24" FontSize="44" HorizontalAlignment="Center"
                               Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                <TextBlock x:Name="txtEmptyTitle" Text="暂无执行历史" Margin="0,12,0,4"
                           FontSize="15" FontWeight="SemiBold" HorizontalAlignment="Center" />
                <TextBlock x:Name="txtEmptyDetail" Text="这个任务还没有写入执行日志。"
                           FontSize="12" HorizontalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource DbkDialogFooterBorderStyle}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock x:Name="txtError" VerticalAlignment="Center" Margin="0,0,12,0"
                           Foreground="{DynamicResource ErrorBrush}" Visibility="Collapsed" />
                <ui:Button Content="关闭" Width="96" Click="Close_Click" />
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>
